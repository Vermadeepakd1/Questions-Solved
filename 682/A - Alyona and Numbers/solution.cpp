#include <bits/stdc++.h>using namespace std; #define endl '\n'#define int long long#define yes cout << "YES" << endl;#define no cout << "NO" << endl;const int MOD = 1e9 + 7;const int INF = LLONG_MAX >> 1; signed main(){    ios::sync_with_stdio(false);    cin.tie(NULL);    int n, m;    cin >> n >> m;     int a = min(n, m), b = max(n, m);    int cnt = 0;    for (int i = 1; i <= a; i++)    {        cnt += (i + b) / 5 - i / 5;        // int r = i % 5;        // int req = 5 - r;         // while (req <= b)        // {        //     cnt++;        //     req += 5;        //     // cout << i << " " << i + req << endl;        // }    }    cout << cnt << endl;}