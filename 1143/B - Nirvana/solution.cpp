#include <bits/stdc++.h>using namespace std; #define endl '\n'#define int long long#define yes cout << "YES" << endl;#define no cout << "NO" << endl;const int MOD = 1e9 + 7;const int INF = LLONG_MAX >> 1; signed main(){    ios::sync_with_stdio(false);    cin.tie(NULL);    int n;    cin >> n;     n++;     int p = 1;    int ans = 0;     auto prod = [](int k)    {        int z = 1;        while (k != 0)        {            z *= k % 10;            k /= 10;        }        return z;    };     while (n != 0)    {        ans = max(ans, prod(n * p - 1));        n /= 10;        p *= 10;    }     cout << ans << endl;}