#include <bits/stdc++.h>using namespace std; #define endl '\n'#define int long long#define yes cout << "YES" << endl;#define no cout << "NO" << endl;const int MOD = 1e9 + 7;const int INF = LLONG_MAX >> 1; signed main(){    ios::sync_with_stdio(false);    cin.tie(NULL);    int tc;    cin >> tc;    while (tc--)    {        int d;        cin >> d;         int a, b;         for (int i = 1 + d;; i++)        {            bool found = true;            for (int j = 2; j * j <= i; j++)            {                if (i % j == 0)                {                    found = false;                    break;                }            }            if (found)            {                a = i;                break;            }        }         for (int i = a + d;; i++)        {            bool found = true;            for (int j = 2; j * j <= i; j++)            {                if (i % j == 0)                {                    found = false;                    break;                }            }            if (found)            {                b = i;                break;            }        }         cout << min(a * b, a * a * a) << endl;    }}